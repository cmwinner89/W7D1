<li>
    <% if action == :show %>
        <%= user.username%>
        <br>
        <a href="<%= users_url %>">Back to Index</a>
    <% else %>
        <%= user.username%> - <a href="<%= user_url(user) %>">Show page</a>
    <% end %>
</li>